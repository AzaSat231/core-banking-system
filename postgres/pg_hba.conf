# Require TLS for all TCP connections (used by middleware / Spring on host ports 5433, 5332).
local   all             all                                     peer
hostssl all             all             all                     scram-sha-256
host    all             all             all                     reject
